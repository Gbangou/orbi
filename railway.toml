[build]
builder = "dockerfile"
dockerfilePath = "deploy/staging/backend.Dockerfile"

[deploy]
preDeployCommand = "pnpm --filter backend prisma:migrate"
startCommand = "node ./apps/backend/dist/src/main.js"
healthcheckPath = ""
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
